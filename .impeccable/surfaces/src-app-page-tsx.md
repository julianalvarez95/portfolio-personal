---
version: 1
slug: "src-app-page-tsx"
primary_target: "src/app/page.tsx"
related_targets: []
---

## Scope

Whole page (`/`), Experience mode with a Persuade obligation.

## Audience, job, action

Technical hiring manager screening for an AI/Platform PM role; will likely open the linked GitHub repo in a second tab. Job: decide whether to reach out. Primary actions, both first-class and never hidden: send a message, download the CV.

## Proof / content

The real live morning-digest feed (Julian's own agent, no-store fetch) is the strongest evidence and must stay legible, not buried. Full career facts in `src/data/` are load-bearing and unchanged.

## Constraints

Dark theme, committed, no toggle. No image generation available this session — no comps, no sketches; execution works from the direction contract alone.

## Chosen direction

EXPLODED VIEW — an assembly-instruction manual rendered in a cyanotype-blueprint register (deep Prussian blue ground, white ruled linework, brick-red/safety-orange for the step being built now, muted brass for held inventory). User-picked challenger, beating the sorteo's assigned SILKSCREEN (PCB schematic) direction. Seed key `48c86ba9`; full contract in `src/app/layout.tsx`.

## Memorable moment

Scrolling assembles the page: each career step clicks into a ghosted, growing structure behind it. The live-digest step is the one that never finishes — its arrow keeps re-triggering because the resource is still actually running.

## Unresolved decisions

- Exact typeface for the blueprint/stencil register — deferred to Fase F, must not default to a training-data mono (IBM Plex banned per `new-work.md` §63).
- How the unlabeled `MOCK_DIGESTS` fallback gets an honest visual form (see PRODUCT.md, Capabilities and Constraints) — the direction must give this a shape, not just the happy path.
